# Journal: Phase 1 bootstrap — 2026-07-15

## Done

- Cargo workspace: `phototux`, `phototux_ui`, `phototux_engine`
- `qtbridge` 0.2 builds against system Qt **6.11.1** (must use `/usr/lib/qt6/bin/qmake` — default `qmake` was Qt 5.15)
- QML shell: toolbar, tool strip, properties dock, status bar, canvas placeholder
- `AppSession` singleton: `brushSize`, `zoom`, `statusText` + slots
- Engine unit tests: 3 passed
- GUI launched; window `PhotoTux` appeared (KWin)

## Cold start (ballpark)

Debug binary; not optimized; not a Phase 5 gate measurement. Use release + profiling later.

Note: ensure `PATH` includes Qt 6 bins before `cargo run`.

## Host notes

```bash
export PATH=/usr/lib/qt6/bin:$PATH
export QMAKE=/usr/lib/qt6/bin/qmake
```

## Next

Phase 2: `phototux_gpu` + `phototux_canvas`, zero-copy risk R2/R3.
