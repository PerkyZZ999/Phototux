# phototux_ui

qtbridge QObjects bound to QML. Package import is `phototux_ui`.

- No wgpu. GPU present stays in `phototux_canvas` / `phototux_gpu`.
- User-facing strings in QML: `qsTr(...)`.
- After QObject API changes, confirm QML still `import phototux_ui`.
