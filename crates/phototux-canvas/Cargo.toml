[package]
name = "phototux_canvas"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true
description = "Production QQuickRhiItem GPU viewport (hybrid C++)"

[dependencies]
phototux_gpu = { path = "../phototux-gpu" }
phototux_engine = { path = "../phototux-engine" }
wgpu = { version = "30.0.0", default-features = true, features = ["vulkan"] }

[build-dependencies]
cc = "1.2.67"
