# Handbook parity P8 exit (2026-07-16)

Clipboard copy refuses >64 MiB; raster adapters keep dimension/alloc limits; `.ptx` skips unknown optional chunks; `extension_data` placeholder for P12. OS MIME bridge deferred.
