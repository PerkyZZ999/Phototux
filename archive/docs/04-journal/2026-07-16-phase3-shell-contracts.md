# Journal — Phase 3 shell contracts (2026-07-16)

## Shipped

- Panel/tool descriptors in `phototux_engine::shell` (+ JSON for QML).
- XDG preferences (`phototux_ui::prefs`) with guides, restore-last-tool, panel visibility.
- Edit → Preferences dialog; Window menu toggles + Reset Workspace.
- Layer row context menu (delete / mask).
- Theme.qml noted as token source of truth.

## Next

Phase 4 engine depth — start with CPU reference composite for tests.
